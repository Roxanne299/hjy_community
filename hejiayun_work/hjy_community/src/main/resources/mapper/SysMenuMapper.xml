<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zgy.hjy_community.system.mapper.SysMenuMapper">
    <select id="getPermissionsBYUserId" resultType="string">
        SELECT sm.perms from sys_user as su
            LEFT JOIN sys_user_role as sur ON su.user_id = sur.user_id
            LEFT JOIN sys_role as sr ON sur.role_id = sr.role_id
            LEFT JOIN sys_role_menu as srm ON sr.role_id = srm.role_id
            LEFT JOIN sys_menu as sm ON srm.menu_id = sm.menu_id
        WHERE sm.status = 0 AND sr.`status` = 0 AND su.status = '0' AND su.del_flag = 0 AND su.user_id = #{userId}
    </select>
    <select id="getAllMenu" resultType="sysMenu">
        SELECT * from sys_menu sm
        WHERE sm.status= 0 AND sm.menu_type IN ('M','C')
        ORDER BY parent_id,order_num
    </select>
    <select id="getMenuById" resultType="sysMenu">
        SELECT sm.* from sys_user as su
            LEFT JOIN sys_user_role as sur ON su.user_id = sur.user_id
            LEFT JOIN sys_role as sr ON sur.role_id = sr.role_id
            LEFT JOIN sys_role_menu as srm ON sr.role_id = srm.role_id
            LEFT JOIN sys_menu as sm ON srm.menu_id = sm.menu_id
        WHERE sm.status= 0 AND sm.menu_type IN ('M','C') AND sr.status = 0 AND su.status = 0 AND su.user_id = #{userId}
        ORDER BY sm.parent_id,sm.order_num
    </select>
</mapper>