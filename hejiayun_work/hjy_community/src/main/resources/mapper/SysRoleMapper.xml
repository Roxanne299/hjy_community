<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zgy.hjy_community.system.mapper.SysRoleMapper">

    <select id="getRolesByUserId" resultType="string">
        SELECT sr.role_key from sys_user as su
            LEFT JOIN sys_user_role as sur ON su.user_id = sur.user_id
            LEFT JOIN sys_role as sr ON sur.role_id = sr.role_id
        WHERE sr.del_flag = 0 AND su.status = '0' AND su.del_flag = 0 AND su.user_id = #{userId}
    </select>
</mapper>